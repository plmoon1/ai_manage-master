分析这个项目的controller接口，在 @docs\interface\ 目录下生成“后端接口文档.md”将每个接口以表格的形式整理出来统一存放到这个文档中
  ，每个表格内容有：接口地址，接口作用，接口参数，接口返回值，备注等。完成以后在 @docs\report\ 目录下生成此次操作的报告（.md格式，带时间戳）。

修改生成的后端接口文档.md，接口返回值有实体类时，如Result.data = List<Permission>，要给出Permission的所有属性。